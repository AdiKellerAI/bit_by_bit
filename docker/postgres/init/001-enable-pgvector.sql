-- Phase 1 (Infrastructure) only: enable the extension the engine needs.
-- Table/schema design is Phase 2 (see PREPARATION-CHECKLIST.md §18) — intentionally not here.
CREATE EXTENSION IF NOT EXISTS vector;
