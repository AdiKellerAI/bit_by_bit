# Local-dev reverse proxy only. No domain/public HTTPS configured yet —
# that's a Phase 3+ concern once the Telegram webhook needs to be reachable
# from the internet (see docs/architecture/adrs/0005-local-docker-runtime.md).
:5678 {
	reverse_proxy n8n:5678
}
